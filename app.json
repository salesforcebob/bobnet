{
  "name": "BobNet Email Simulator",
  "description": "Heroku app to simulate email opens/clicks from inbound emails via CloudMailIn or Mailgun (FastAPI + RQ).",
  "repository": "",
  "keywords": ["fastapi", "heroku", "cloudmailin", "mailgun", "rq", "redis"],
  "env": {
    "WEBHOOK_SECRET": {
      "description": "(optional) Shared secret for CloudMailIn webhook (X-Webhook-Secret header).",
      "required": false
    },
    "MAILGUN_SIGNING_KEY": {
      "description": "(optional) Mailgun HTTP webhook signing key for signature verification. Get from Mailgun dashboard > Settings > API Security.",
      "required": false
    },
    "MAILGUN_DOMAIN": {
      "description": "(optional) Restrict Mailgun webhook to recipients at this domain (e.g., inbound.example.com).",
      "required": false
    },
    "SIMULATE_WITH_BROWSER": {
      "description": "Use headless browser for opens (false = direct fetch).",
      "required": false,
      "value": "false"
    },
    "SIMULATE_OPEN_PROBABILITY": {"value": "0.7"},
    "SIMULATE_CLICK_PROBABILITY": {"value": "0.3"},
    "MAX_CLICKS": {"value": "2"},
    "OPEN_DELAY_RANGE_MS": {"value": "500,5000"},
    "CLICK_DELAY_RANGE_MS": {"value": "300,4000"},
    "REQUEST_TIMEOUT_MS": {"value": "8000"}
  },
  "formation": {
    "web": {"quantity": 1, "size": "basic"},
    "worker": {"quantity": 1, "size": "basic"}
  },
  "addons": [
    {"plan": "cloudmailin:starter"},
    {"plan": "heroku-redis:mini"}
  ],
  "buildpacks": [
    {"url": "heroku/python"}
  ]
}
